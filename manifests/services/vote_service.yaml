apiVersion: v1
kind: Service
metadata:
  name: vote-service
  namespace: voting-app
spec:
  type: ClusterIP
  selector:
    app: voting
    microapp: voting-frontend
  ports:
    - name: voting-port
      port: 80
      targetPort: 80
      